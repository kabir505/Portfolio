from collections import Counter
from collections import deque
scores = Counter()
scores.update(your_data_here)
_names_set = set(names)
queue = []
queue.append('task')
queue.popleft()
names = ['Alice', 'Bob', 'Charlie']
if 'Charlie' in _names_set:
    print('Found')
